#pragma once
#ifndef GAME_STATE_H
#define GAME_STATE_H

// Define an enum to represent different game states
enum class GameStatesEnum
{
    MainMenu,
    Playing,
    GameOver
};

#endif // GAME_STATE_H